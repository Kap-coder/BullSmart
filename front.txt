**Contexte & Objectif :**
Je développe une application complète de gestion des bulletins scolaires Camerounais. Le frontend doit être construit avec **React 18 + Vite + TailwindCSS + shadcn/ui + Axios + React Router + Zustand (ou Redux Toolkit)**.
Il doit consommer l’API Django/DRF (backend déjà décrit dans le super prompt précédent).
L’IA doit générer le code étape par étape, avec une structure claire, des explications, et une UI moderne, responsive et professionnelle.

**Contraintes :**

* Respect strict du design simple, professionnel et lisible (tableaux, graphiques, exports).
* Gestion multi-rôles : Admin, Directeur, Enseignant, Parent, Élève (UI adaptée selon rôle).
* Connexion au backend via JWT (login, logout, refresh token).
* Utilisation de TailwindCSS + composants shadcn/ui (boutons, formulaires, cartes, modales).
* Graphiques dynamiques avec **Chart.js ou Recharts**.
* Support multi-device (responsive web + installable PWA).
* Fonctionnalités IA accessibles depuis le frontend (analyse, recommandations).

---

### 🔑 Étapes demandées à l’IA

1. **Initialisation du projet :**

   * Créer projet avec `npm create vite@latest bulletin-frontend --template react`.
   * Configurer TailwindCSS + shadcn/ui.
   * Configurer React Router v6.
   * Installer Axios et Zustand (ou Redux Toolkit).
   * Créer une structure de base :

     ```
     src/
       api/
       components/
       hooks/
       layouts/
       pages/
       store/
       utils/
     ```

2. **Authentification & State Management :**

   * Page Login + Register.
   * Connexion au backend via Axios.
   * Gestion JWT (stockage dans localStorage + refresh automatique).
   * Store global pour l’utilisateur (rôle, infos, token).

3. **Layouts & Navigation :**

   * Layout principal avec **Sidebar + Header**.
   * Navigation dynamique en fonction du rôle.
   * Exemple :

     * Admin → Gestion utilisateurs, classes, matières.
     * Enseignant → Notes, bulletins.
     * Parent/Élève → Consultation bulletin.

4. **Pages principales (CRUD) :**

   * **Élèves** : liste, ajout, édition, suppression.
   * **Classes** : liste, ajout, édition.
   * **Matières** : gestion matières + coefficients.
   * **Notes** : saisie et modification par enseignant.
   * **Bulletins** : génération, affichage, export (PDF/Excel).
   * **Tableaux + formulaires** avec composants shadcn/ui.

5. **Affichage des Bulletins :**

   * Page détail bulletin (avec matières, notes, moyenne, rang, mention).
   * Téléchargement PDF via backend.
   * Affichage graphique (progression, histogrammes, comparaisons).

6. **Tableaux de Bord & Statistiques :**

   * Dashboard par rôle :

     * Admin : stats globales (nb élèves, enseignants, classes).
     * Enseignant : progression par matière.
     * Élève/Parent : suivi notes + recommandations IA.
   * Graphiques dynamiques avec Recharts.

7. **Intégration IA :**

   * Page “Analyse” :

     * Élève : recommandations pour progresser.
     * Enseignant : analyse des tendances de classe.
   * Requêtes envoyées à l’endpoint IA du backend → affichage clair et graphique.

8. **Sécurité & UX :**

   * Routes protégées (redirect si pas connecté).
   * Validation des formulaires.
   * Notifications (shadcn Toast).
   * Dark mode (optionnel).

9. **Optimisation & PWA :**

   * Transformer l’app en PWA (installable sur machine/école).
   * Optimisation performance (lazy loading, code splitting).

10. **Tests & Livraison :**

    * Tests unitaires (React Testing Library).
    * Vérification navigation et API calls.
    * Build optimisé pour production.

---

### ✅ Format de réponse attendu de l’IA

* Fournir le code complet de chaque étape (App.jsx, pages, composants).
* Ajouter explications claires.
* Fournir exemples d’utilisation (login, affichage d’un bulletin).
* Ne pas sauter d’étapes, générer étape par étape.
* Donner bonnes pratiques (clean code, responsive, accessibilité).
* Indiquer la prochaine étape à chaque fin de réponse.

---

👉 Avec ce duo de **super prompts backend + frontend**, ton IA pourra générer **toute l’application de gestion de bulletins scolaires**, clé en main.

Veux-tu que je prépare aussi un **planning de dev (roadmap semaine par semaine)** pour que tu saches dans quel ordre attaquer backend et frontend ?
